<div
    id="user-badge"
    class="h-12 w-12 p-2 bg-yellow-500 rounded-full text-white font-semibold flex items-center justify-center"
    data-username="{{ app.user.chatUsername }}"
>
    {{ app.user.chatUsername|first }}
</div>
